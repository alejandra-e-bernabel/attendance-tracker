{
  "name": "attendance-tracker",
  "version": "1.0.0",
  "description": "Attendance tracking system with QR codes",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd attendance-tracker-backend && npm run dev",
    "dev:frontend": "cd attendance-tracker-frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
